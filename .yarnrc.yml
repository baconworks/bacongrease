# Use a real node_modules tree (not Plug'n'Play). The component SCSS resolves
# the design system via Vite/sass loadPaths pointing at packages/styles/src;
# PnP has no on-disk node_modules for that to sit alongside, so node-modules
# linker is required here. See CLAUDE.md "How SCSS resolution works".
nodeLinker: node-modules
